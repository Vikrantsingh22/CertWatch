import { RuleResult } from './rules/domain-age.rule';

export function buildReasons(
  ruleResults: Array<{ points: number; reason: string | null }>
): string[] {
  // Stub implementation
  return [];
}
